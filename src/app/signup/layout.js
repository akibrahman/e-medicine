export const metadata = {
    title: "Sign Up || E-Medicine",
    description: "This is Sign Up page",
  };
  
  const layout = ({ children }) => {
    return <div>{children}</div>;
  };
  
  export default layout;
  