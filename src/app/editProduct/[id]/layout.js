export const metadata = {
  title: "Edit Product || E-Medicine",
  description: "This is edit product page",
};
const layout = ({ children }) => {
  return <div>{children}</div>;
};

export default layout;
