export const metadata = {
    title: "Login || E-Medicine",
    description: "This is Login page",
  };
  
  const layout = ({ children }) => {
    return <div>{children}</div>;
  };
  
  export default layout;
  